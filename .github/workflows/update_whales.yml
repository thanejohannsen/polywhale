name: Update Whale List

on:
  schedule:
    - cron: '0 6 */3 * *'   # every 3 days at 6am UTC
  workflow_dispatch:          # manual trigger from GitHub Actions tab

jobs:
  update:
    runs-on: ubuntu-latest
    permissions:
      contents: write         # needed to push the updated JSON back

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'

      - name: Install dependencies
        run: pip install requests

      - name: Run WhaleStrategy
        run: python WhaleStrategy.py

      - name: Fetch categories
        run: python fetch_categories.py

      - name: Commit updated files
        run: |
          git config user.name  "github-actions[bot]"
          git config user.email "github-actions[bot]@users.noreply.github.com"
          git add qualified_whales.json categories.json
          git diff --staged --quiet || git commit -m "chore: update whales and categories [skip ci]"
          git push
